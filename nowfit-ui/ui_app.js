import React from "react";
import ReactDOM from "react-dom/client";
import NowfitMockup from "./components/NowfitMockup";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <NowfitMockup />
  </React.StrictMode>
);
